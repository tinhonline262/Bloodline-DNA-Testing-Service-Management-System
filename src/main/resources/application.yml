server:
  port: 8080
  # servlet:
  #   context-path: /v1    # Comment dòng này để test

spring:
  application:
    name: dev

  thymeleaf:
    cache: false

  # Cấu hình welcome page
  web:
    resources:
      static-locations: classpath:/static/
  mvc:
    view:
      prefix: /
      suffix: .html

  datasource:
    url: "jdbc:mysql://localhost:3306/dna_testing_system"
    username: hil
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect